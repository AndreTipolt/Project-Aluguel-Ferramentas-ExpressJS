<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alugar Ferramenta</title>
</head>
<body>  

    <form action="/rentTool/create" method="post">
        <label for="">Cliente: </label>
        <select name="idClient">
            {{#each clients}}
                <option value="{{ this.idClient }}">{{ this.name }}</option>
            {{/each}}
        </select> <br>

        <label for="">Ferramenta</label>
        <select name="idTool">
            {{#each tools}}
                <option value="{{ this.idTool }}">{{ this.name }}</option>
            {{/each}}
        </select><br>

        <label for="">Data da devolução</label> 
        <input type="date" name="devolutionDate">

        <button>Enviar</button>
    </form>
</body>
</html>
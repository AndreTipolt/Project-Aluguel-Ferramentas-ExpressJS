<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alugar Ferramenta</title>
    <link rel="stylesheet" href="/css/creates.css">
</head>

<body>

    <h1 style="text-align: center; margin: 10px 0 25px 0;" class="display-3">Alugar Ferramenta</h1>
    <main>
        <div class="form">

            {{#if msgError}}

            <label for="messageError" class="msg-form-error">{{ msgError }}</label>

            {{/if}}

            {{#if msgSucess }}

            <label for="messageSucess" class="msg-form-sucess">{{ msgSucess }}</label>
            {{/if}}

            <form action="/rentTool/create" method="post">
                <label for="">Cliente: </label>
                <select name="idClient" class="form-select" aria-label="Default select example">
                    <option selected disabled>Selecione o Cliente</option>

                    {{#each clients}}
                        <option value="{{ this.idClient }}">{{ this.name }}</option>
                    {{/each}}

                </select> <br>

                <label>Ferramenta:</label>
                <select name="idTool"class="form-select" aria-label="Default select example">
                    <option selected disabled>Selecione a Ferramenta</option>

                    {{#each tools}}
                        <option value="{{ this.idTool }}">{{ this.name }}</option>
                    {{/each}}

                </select><br>

                <label>Data da devolução:</label><br>
                <input type="date" name="devolutionDate" class="date-input-form"> <br>

                <button type="submit" class="btn btn-primary btn-submit" style="margin-top: 0px">Alugar</button>
            </form>
        </div>
    </main>
</body>

</html>